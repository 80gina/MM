# 로컬 데이터 확인

실제 경로는 C:\Users\yello\코디세이\team_motion 입니다.
사용자가 알려준 team\_motion과 달리 team_motion이라는 하나의 폴더입니다.

2026-09-19 확인 결과:
- 성인 ‘감정이 태깅된 자유대화’: 압축 해제 JSON 186개, ZIP 2개.
- 청소년 ‘감정이 태깅된 자유대화’: 압축 해제 JSON 923개, ZIP 2개.
- 압축 파일과 해제 파일이 겹칠 수 있으므로 합산한 표본 수가 아닙니다.
- 그룹별 첫 JSON의 구조를 확인했습니다. 전체 품질 검증·학습·모델 성능 평가를 수행한 것은 아닙니다.

Conversation 배열 안에 Text, SpeakerEmotionObject, VerifyEmotionObject, SpeakerEmotionCategory, VerifyEmotionCategory, 감정 강도 등의 항목이 있습니다.
원본에는 화자·음성 메타데이터도 있습니다. 구조 보고서에는 항목 이름만 기록하고 대화 원문이나 개인 속성 값은 공개하지 않습니다.

모델 카드의 학습 데이터인 ‘감성대화말뭉치’와 이번 데이터 이름은 다릅니다. 동일 데이터라고 설명하지 않습니다.
중립·두려움 등의 라벨이 관찰되어 모델의 불안·기쁨 등과 바로 일치하지 않습니다. 다음 검증 단계에서는 데이터 가이드의 라벨 정의를 확인하고, 비교 가능한 항목과 비교 불가능한 항목을 분리해야 합니다. 특히 중립을 기쁨으로 자동 변환하지 않습니다.

2026-09-19 전수 집계 결과 JSON 2,335개, 유효 대화 파일 2,333개, 발화 847,156건, 빈 문장 0건, 손상 JSON 0건을 확인했다. 검증 감정 대상 분포는 기쁨 424,411, 없음 206,219, 놀라움 84,761, 화남 43,163, 사랑스러움 34,624, 슬픔 34,052, 두려움 19,926건이다.

현재 반영: 전수 라벨 통계, 대화 파일 단위 80/10/10 분리, 대응 가능한 5개 감정의 균형 표본 파일럿 추가 학습.
미반영: `상처` 신규 표본, 대규모 최종 학습, 원본 데이터 업로드. `없음`과 `사랑스러움`은 6개 감정에 억지로 연결하지 않고 제외했다.

재현 명령: `python inspect_data.py "로컬 데이터 폴더"`
결과: evidence/data-schema.json

전수 통계: `python analyze_training_data.py "데이터 폴더"`
결과: `evidence/training-data-audit.json`

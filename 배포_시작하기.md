# 배포 시작하기 — 클릭만 따라 하면 됩니다

명령어(터미널) 없이 **웹브라우저만으로** 배포합니다.
전체 20분. 실제로 손이 가는 건 5분이고 나머지는 기다리는 시간입니다.

준비물: 인터넷, Hugging Face 계정(`yellowmug` — 이미 있음), 코디세이에서 받은 API 값

---

## 준비 0. 비밀번호 먼저 바꾸기

대화창에 비밀번호를 적으셨으니 먼저 바꿔 주세요.

1. https://huggingface.co/settings/account 접속
2. **Password** 항목에서 변경

---

## 1단계. Space 만들기 (2분)

Space는 "내 서비스를 올려두는 공간"입니다. 한 번 만들면 주소가 생깁니다.

1. https://huggingface.co/new-space 접속
2. 아래처럼 입력합니다.

| 칸 | 넣을 값 | 주의 |
|---|---|---|
| **Owner** | `yellowmug` | 그대로 둡니다 |
| **Space name** | `mindily` | ⚠️ **반드시 소문자 mindily**. 다르면 주소가 달라집니다 |
| **License** | `mit` | 목록에서 고릅니다 |
| **Select the Space SDK** | **Docker** → 그 아래 **Blank** | ⚠️ Gradio·Streamlit 아님 |
| **Space hardware** | `CPU basic · Free` | 기본값 그대로 |
| **Public / Private** | **Public** | 평가자가 열어야 하니 Public |

3. 맨 아래 **Create Space** 클릭

→ 화면이 바뀌면서 "이제 파일을 올리세요" 같은 안내가 나옵니다. 여기까지 성공입니다.

> ⚠️ **"Welcome to your static Space!"라고 뜬 경우**
> SDK가 Static으로 만들어진 것입니다. 지우고 다시 만들 필요 없습니다.
> 3단계에서 파일을 올리면 `README.md` 안의 `sdk: docker` 설정 때문에 **자동으로 Docker Space로 바뀝니다.**
> 단, 이때는 13개 파일을 **한 번에** 올려야 합니다. README만 먼저 올리면 Dockerfile이 없어서 빌드가 실패합니다.

---

## 2단계. 코디세이 API 값 등록하기 (2분)

생성형 AI를 켜는 단계입니다. **이걸 안 해도 서비스는 돌아갑니다** (코치 문장만 규칙 기반으로 나옴). 코디세이에서 받은 값이 아직 없으면 건너뛰고 나중에 해도 됩니다.

1. 방금 만든 Space 화면 오른쪽 위 **Settings** 클릭
2. 아래로 내려 **Variables and secrets** 찾기
3. **New secret** 버튼을 눌러 하나 추가합니다.

| Name | Value | 버튼 |
|---|---|---|
| `CODYSSEY_API_KEY` | 코디세이에서 받은 키 | **New secret** |

4. 이어서 **New variable** 버튼으로 두 개 더 추가합니다.

| Name | Value | 버튼 |
|---|---|---|
| `CODYSSEY_API_BASE` | 코디세이에서 받은 주소 (예: `https://api.codyssey.kr/v1`) | **New variable** |
| `CODYSSEY_MODEL` | 코디세이에서 지정한 모델명 (예: `gpt-4o-mini`) | **New variable** |

> **secret과 variable의 차이**: 남에게 보이면 안 되는 건 secret(키), 보여도 되는 건 variable(주소·모델명)입니다.

---

## 3단계. 파일 올리기 (3분)

올릴 파일은 이미 한 폴더에 모아 두었습니다.

📁 **`C:\Users\yello\코디세이\MM\_space_upload`**

이 폴더 안의 것을 **전부** 올리면 됩니다. 13개 파일입니다.

1. Space 화면 위쪽 탭에서 **Files** 클릭
2. 오른쪽 **⊕ Add file** → **Upload files** 클릭
3. 탐색기에서 위 `_space_upload` 폴더를 엽니다
4. 폴더 안의 **파일 10개와 `dist` 폴더**를 모두 선택해서 (Ctrl+A) 브라우저 화면으로 **드래그**합니다
5. 목록에 파일들이 뜨면, 아래 **Commit changes to main** 버튼 클릭

⚠️ **반드시 13개를 한 번에 올리고 한 번에 Commit** 하세요. 나눠 올리면 중간에 빌드가 실패합니다.

올라가야 할 목록:

```
Dockerfile
README.md
requirements.txt
server.py
coach_agent.py
rag.py
llm.py
feedback_db.py
healing_knowledge.py
memory_db.py
dist/app.js
dist/index.html
dist/styles.css
```

> **dist 폴더가 안 올라갈 때**: 폴더 드래그가 안 되면, 먼저 파일 10개만 올리고 → 다시 **⊕ Add file → Upload files** → `dist` 폴더 안으로 들어가 파일 3개를 선택 → 올리기 전에 위쪽 경로 칸에 `dist/`를 입력하면 됩니다.

---

## 4단계. 빌드 기다리기 (10~15분)

파일을 올리면 자동으로 빌드가 시작됩니다.

- 화면 위에 **Building** 이라는 주황색 표시가 뜹니다
- **Logs** 탭을 누르면 진행 상황이 글자로 흘러갑니다
- 처음엔 오래 걸립니다. PyTorch를 설치하고 감정 분석 모델(약 450MB)을 이미지에 담기 때문입니다
- 표시가 **Running**(초록색)으로 바뀌면 완료입니다

이 시간 동안 다른 일 하셔도 됩니다. 창을 닫아도 빌드는 계속됩니다.

---

## 5단계. 확인하기 (1분)

주소창에 입력합니다.

**https://yellowmug-mindily.hf.space**

앱 화면이 뜨면 성공입니다. 일기를 하나 써보고 감정 분석까지 되는지 확인해 보세요.

생성형 AI가 켜졌는지 보려면 뒤에 `/api/llm/status`를 붙입니다.

**https://yellowmug-mindily.hf.space/api/llm/status**

| 나오는 글자 | 뜻 |
|---|---|
| `"mode": "llm_grounded"` | ✅ 코디세이 생성형 AI 작동 중 |
| `"mode": "deterministic_fallback"` | 키가 없거나 틀림 → 2단계 다시 확인 (서비스 자체는 정상) |

---

## 문제가 생기면

| 증상 | 원인 | 해결 |
|---|---|---|
| **Build failed** (빨간 표시) | 파일이 빠졌을 가능성 | Logs 탭에서 `No such file` 글자를 찾아 그 파일을 다시 업로드 |
| 화면은 뜨는데 분석이 안 됨 | 모델이 아직 로딩 중 | 1~2분 기다렸다 새로고침 |
| `README.md` 오류 | Space용 README가 아님 | `_space_upload/README.md`를 올렸는지 확인 (맨 위에 `title: Mindily`로 시작해야 함) |
| 주소가 안 열림 | Space 이름이 `mindily`가 아님 | Settings에서 이름 확인 |
| 한참 안 쓰다 열었더니 느림 | 무료 Space는 48시간 후 절전 | 발표 10분 전에 미리 한 번 열어서 깨워두기 |

---

## 배포가 끝난 뒤

1. 휴대폰으로 주소를 열어 **화면 캡처** → 제출 증빙
2. 친구·가족 **5명에게 주소 전달** → 사용자 테스트 (이게 마지막 감점 요인입니다)
3. 발표자료 12장 만들기 → `docs/발표자료구성안.md` 참고

### 사용자 테스트 부탁 문구 (그대로 복사해서 쓰세요)

> 안녕하세요, 학교 과제로 만든 감정 일기 서비스예요.
> 3분만 써보고 4가지만 점수 매겨 주실 수 있을까요?
>
> 👉 https://yellowmug-mindily.hf.space
>
> 1. 아래 문장을 그대로 입력해 주세요: **"내일 발표를 잘할 수 있을지 걱정돼요."**
> 2. 스트레스는 3으로 선택
> 3. 분석 결과와 추천 활동을 확인
>
> 그다음 아래 4가지를 1~5점으로 알려주세요.
> ① 분석 결과가 이해되었나요? ② 공감이 되었나요?
> ③ 추천 활동이 쓸모 있었나요? ④ 화면이 보기 편했나요?
>
> 불편했던 점도 한 줄 적어주시면 고쳐서 반영하겠습니다.
> (일기 내용은 저장되지 않고, 답변은 익명으로만 씁니다.)

답변을 받으시면 저에게 알려주세요. `docs/피드백기록.md`와 결과보고서 6절에 정리해 드리겠습니다.

# 배포와 사용자 테스트 — 지금 상태와 할 일

마지막 갱신: 2026-09-20

## 지금 상태 한눈에

| 항목 | 상태 |
|---|---|
| 앱 코드 | ✅ 완성 (감정 분석 · Agent · RAG · 생성형 AI · 기억 · 자연의 소리) |
| 발표자료 | ✅ 12장 완성 — 10장(피드백)과 이름 칸만 비어 있음 |
| 설치형 앱(PWA) | ✅ 홈 화면 설치 · 오프라인 화면 동작 확인 |
| 로컬 실행 | ✅ 동작 확인 |
| GitHub 반영 | ⬜ 커밋 몇 개가 남아 있음 — `push_to_github.cmd` |
| Hugging Face Space | ✅ 최신본 20개 파일 반영 완료 — 실행만 쿼터 해제 대기 |
| 사용자에게 배포 | ✅ **같은 와이파이 방식으로 오늘 가능** |

---

## 1. GitHub에 올리기 (1분)

탐색기에서 더블클릭:

```
push_to_github.cmd
```

로그인 창이 뜨면 GitHub 계정으로 로그인합니다. `[OK]`가 뜨면 끝입니다.

---

## 2. 사용자에게 배포하기 — 두 가지 방법

### 방법 A. 같은 와이파이 (오늘 바로 가능 · 추천)

탐색기에서 더블클릭:

```
SHARE_ON_WIFI.cmd
```

화면에 나오는 주소를 테스터에게 전달합니다.

```
http://192.168.0.XX:8020
```

- 같은 와이파이에 있는 휴대폰이면 바로 열립니다
- Windows가 네트워크 접근을 물으면 **허용**을 누릅니다
- 검은 창을 닫으면 주소가 죽습니다. 테스트 동안 열어두세요

### 방법 B. 인터넷 주소 (터널)

```
SETUP_AND_SHARE.cmd
```

`https://....trycloudflare.com` 주소가 나오면 어디서든 접속됩니다.
실패하면 `FIX_TUNNEL.cmd`를 쓰고, 그래도 안 되면 네트워크가 막는 것이니
**휴대폰 핫스팟**에 노트북을 연결해 다시 시도하세요.

---

## 3. Hugging Face Space 최신본 올리기 (완료)

**2026-09-20 기준 Space에는 현재 빌드 20개 파일이 모두 올라가 있습니다.** 처음 Space를 만들 때 생긴 찌꺼기 `style.css`도 정리했습니다.
앞으로 코드를 고친 뒤에도 같은 방법으로 올리면 됩니다.

### 올리는 방법 — 더블클릭 한 번

탐색기에서:

```
push_to_huggingface.cmd
```

로그인 창이 뜨면 이렇게 넣습니다.

| 칸 | 넣을 값 |
|---|---|
| Username | `yellowmug` |
| Password | **Hugging Face 쓰기(write) 토큰** |

> 토큰은 https://huggingface.co/settings/tokens 에서 **New token → Write** 로 만듭니다.
> 계정 비밀번호는 더 이상 받지 않습니다. 토큰은 저에게 보내지 마세요.

끝나면 `[DONE]`이 뜹니다. 무엇이 바뀌었는지 목록도 화면에 나옵니다.
이 방법은 20개 파일을 손으로 드래그하는 것보다 빠르고, 지워야 할 파일도 같이 정리됩니다.

### 올린 뒤

Space 화면 → **Logs** 탭에서 빌드가 도는지 봅니다.

> ⚠️ 지금은 플랫폼의 `CPU Basic quota` 오류로 컨테이너가 **기동하지 않습니다.**
> 신규 무료 계정에서 널리 보고된 문제이고, 지원팀(website@huggingface.co)에 요청한 상태입니다.
> **파일을 올려두는 것과 실행되는 것은 별개**입니다. 쿼터가 풀리면 올려둔 최신본으로 바로 빌드됩니다.
> 그때까지 사용자 테스트는 위 2번(같은 와이파이)으로 진행하시면 됩니다.

### 손으로 올리고 싶다면

`_space_upload` 폴더의 **20개 파일**을 Files 탭 → **+ Contribute → Upload files** 로 전부 한 번에 올립니다.
폴더는 만들지 않고 평평하게 올립니다. 이 경우 기존 `style.css`는 따로 지워야 합니다.

---

## 3.5 휴대폰에 앱으로 설치하기 (PWA)

Mindily는 **설치형 웹앱**입니다. 앱스토어 없이 홈 화면에 아이콘이 생깁니다.
위 2번의 와이파이 주소든 Space 주소든, 주소만 열면 동일하게 설치됩니다.

| 기기 | 방법 |
|---|---|
| **안드로이드 · Chrome** | 화면 아래 "홈 화면에 추가하면 앱처럼 열려요" 안내 → **설치** |
| **아이폰 · Safari** | 아래 **공유(↑)** → **홈 화면에 추가** |
| **PC · Chrome/Edge** | 주소창 오른쪽 **설치 아이콘** 클릭 |

설치 후에는 주소창 없이 전체 화면으로 열리고, 인터넷이 끊겨도 화면은 그대로 뜹니다.
감정 분석은 서버가 필요하므로 연결된 뒤에 동작하며, 일기 원문은 원래부터 기기 안에만 저장됩니다.

> 테스터에게는 "주소를 열고 홈 화면에 추가해 주세요"라고 한 줄만 안내하면 됩니다.

---

## 4. 사용자 테스트 진행 (1인당 5분)

### 참가자에게 이렇게 안내하세요

> 학교 과제로 만든 감정 일기 앱이에요. 5분만 써보고 네 가지만 점수 매겨 주세요.
> 진짜 개인 일기는 안 쓰셔도 되고, 제가 드리는 문장을 그대로 넣으시면 됩니다.
> 의료 진단 앱이 아니고, 답변은 익명으로만 씁니다. 중간에 그만두셔도 괜찮아요.

### 공통 시나리오

1. 아래 문장을 그대로 입력 — **`내일 발표를 잘할 수 있을지 걱정돼요.`**
2. 스트레스 **3** 선택 → 분석
3. 감정 레이더와 추천 꽃·향 확인
4. 무드 미터에서 본인 감정을 직접 고르기
5. AI 코치에서 활동 탭 하나를 골라 실제로 해보기 (자연의 소리 권장)
6. 앱 안에서 **만족도 남기기**

### 네 가지 질문 (각 1~5점)

| 항목 | 질문 |
|---|---|
| 이해도 | 분석 결과의 뜻을 이해하기 쉬웠나요? |
| 공감도 | 문구가 내 감정을 존중한다고 느꼈나요? |
| 추천 | 추천 활동이 상황에 어울렸나요? |
| 접근성 | 글자·버튼·화면 이동이 쓰기 편했나요? |

### 마지막 질문 (가장 중요)

> 가장 불편했던 점 하나만 말씀해주세요.

결과는 `docs/사용자테스트기록지.md`에 **U01~U05 코드로만** 적습니다. 이름은 적지 않습니다.

---

## 5. 테스트 후

1. 받은 점수와 불편 사항을 정리
2. 고칠 수 있는 것은 고치고 **개선 전/후 화면 캡처**
3. `docs/피드백기록.md`와 `docs/결과보고서.md` 6절 채우기
4. 발표자료 10장(피드백 개선)의 재료로 사용

개선 전후 한 쌍이 발표 배점의 핵심입니다. 한 건이라도 꼭 만드세요.
